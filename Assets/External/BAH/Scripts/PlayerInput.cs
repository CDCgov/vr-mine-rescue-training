using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using OVR;

namespace BAHDOL
{
    public class PlayerInput : MonoBehaviour
    {
        // Update is called once per frame
        void Update()
        {




            // We toggle the AI follow functionality here. 
            // Button.Two is the "B" button on the right controller

            //if (OVRInput.GetDown(OVRInput.Button.Two))
            //{
            //    Debug.Log("toggling");
            //    NPCManager.Instance.followToggle.Invoke();
            //}
        }

    }
}
