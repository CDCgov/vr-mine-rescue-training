using UnityEngine;
using System.Collections;

public class DemoProxLog : MonoBehaviour {

    public ProximityDataLogHandle proxLog;
    //void OnTriggerEnter(Collider other)
    //{
    //	PlayerLogHandle player = other.gameObject.GetComponent<PlayerLogHandle>();
    //	if(player != null)
    //	{
    //		proxLog.ProximityEvent(player.PlayerID, other.transform.position);
    //	}
    //}
}
