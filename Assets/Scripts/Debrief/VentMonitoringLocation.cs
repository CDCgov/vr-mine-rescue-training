using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VentMonitoringLocation : MonoBehaviour
{
    public Vector3 LabelOffset;

    // Start is called before the first frame update
    void Start()
    {
        
    }
}
